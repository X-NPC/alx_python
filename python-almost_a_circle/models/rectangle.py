import base

class Rectangle (base):
    """ private instance attributes, each with its own getters and setters"""
    # do what the doc strin says and rewrite it.
    
    def __init__(self, width, height, x=0):
        #call super class id 
        return # delete this. did it because I didnt' like the red thing
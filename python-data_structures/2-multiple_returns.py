def multiple_returns(sentence):
    length = len(sentence)
    if legth == 0:
        length = 0
        first = None
        return lenght, first
    else:
        splitted = [ x for x in sentence ]
        first = splitted [0]
        return length, first

